PORT=5000
MONGO_URI=mongodb://localhost:27017/hrms
JWT_SECRET=change-me
JWT_EXPIRES_IN=7d

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="HRMS <no-reply@hrms.local>"

# Base URL where the frontend HTML pages are served from — used to build the
# password-reset link emailed by /forgot-password (points to <FRONTEND_URL>/resetpassword.html).
FRONTEND_URL=http://localhost:5500
